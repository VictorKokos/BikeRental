<Window x:Class="BikeRental.View.SignInView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BikeRental.View"
        mc:Ignorable="d"
        Title="SignInView" Height="350" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="200"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="10">
            <Label Content="Login:"/>
            <TextBox Text="{Binding UserAccount.UserLogin}"/>
            <Label Content="Password:"/>
            <PasswordBox x:Name="passwordBox"/>
        </StackPanel>
        <Button Grid.Row="1" Margin="10" Command="{Binding SignInCommand}" CommandParameter="{Binding ElementName=passwordBox}" Content="Sign In" Click="SignInButton_Click">
        </Button>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Don't have an account? "/>
            <Button Content="Sign Up" Click="SignUpButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
